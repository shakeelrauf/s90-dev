<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no"/>
  <meta name="description" content=""/>
  <meta name="author" content=""/>
  <title>S90 <%= yield(:title) %> </title>
  <!--favicon-->
  <link rel="icon" href="<%= image_url('favicon.ico')%>" type="image/x-icon">
  <%= stylesheet_link_tag  "application", :media => "all" %>
  <link href="/s92/css/font-awesome.min.css" rel="stylesheet">
  <%= javascript_include_tag  "application2", "data-turbolinks-track" => true  %>
  <script type="text/javascript" src="/s92/js/jquery.validate.js"></script>
  <%= yield(:head) %>

  <% if (content_for?(:custom_css)) %>
    <%= yield(:custom_css) %>
  <% end %>
</head>

<body >
  <%= render partial: "security/pid" %>

  <div id="wrapper">
    <div id="pcoded">

      <%= render partial: 'shared/partials/side_bar' %>
      <%= render partial: 'shared/partials/nav_bar2' %>
      <div class="clearfix"></div>
      <%= yield(:bc) %>

      <div class="content-wrapper">
        <div class="container-fluid">
          <% if (content_for?(:content)) %>
            <%= yield(:content) %>
          <% else %>
            <%= yield %>
          <% end %>
        </div>
      </div>
      <script type="text/javascript" src="/s92/js/page_init.js"></script>
      <script type="text/javascript" src="/s92/js/all.js"></script>
      <%= render partial: 'shared/partials/footer' %>
    </div>

  </div>

<%#= render "shared/partials/player_bar" %>
<%= render "shared/partials/browser_warnings" %>
<%= javascript_include_tag 'onerror.js' %>
<%= yield(:custom_js) %>
<% p = controller.js_path %>
<% if (p.present?) %>
  <script src="<%= p %>?<%= Constants::TS %>"></script>
<% end %>

</body>

</html>
