<!DOCTYPE html>
<html lang="en">

<head>
    <%= render "shared/partials/title_meta" %>
    <%= csrf_meta_tags %>
    <% if (content_for?(:custom_css)) %>
      <%= yield(:custom_css) %>
    <% end %>

    <%= render "shared/partials/base_css" %>
</head>

<body style="display:none">
  <%= render partial: "security/pid" %>

  <!-- [ Pre-loader ] start -->
  <div class="loader-bg">
      <div class="loader-bar"></div>
  </div>
  <!-- [ Pre-loader ] end -->

  <div id="pcoded" class="pcoded" style="padding-bottom:200px">
      <div class="pcoded-overlay-box"></div>
      <div class="pcoded-container navbar-wrapper">
        <%= render "shared/partials/header" %>

        <%= render "shared/partials/chat" %>

          <div class="pcoded-main-container">
              <div class="pcoded-wrapper">
                <%= render "shared/partials/nav_bar" %>

                  <div class="pcoded-content">
                      <%= yield(:bc) %>
                      <div class="pcoded-inner-content">
                      <%= yield(:content) %>
                      <!--
                      <%= render "shared/partials/style_select" %>
                    -->
                  </div>
              </div>
          </div>
      </div>
  </div>
</div>

<!--
<div class="div-player">
  <iframe src="/" height="200">
  </iframe>
<div>
-->

  <!-- Footer Section -->
  <!--
  <div class="footer">
      <div class="container">
          <div class="col-md-12 text-center">
              <img src="assets/logos/logo.png" alt="Able 7.0 Logo" />
              <ul class="footer-menu">
                  <li><a href="http://demo.com">Site</a></li>
                  <li><a href="#">Support</a></li>
                  <li><a href="#">Terms</a></li>
                  <li><a href="#">Privacy</a></li>
              </ul>
              <div class="footer-text">
                  <p>
                      Copyright © 2018 Able 7.0. All Rights Reserved.
                  </p>
              </div>
          </div>
      </div>
  </div>
-->

<%= render "shared/partials/browser_warnings" %>
<%= render "shared/partials/base_js" %>

  <%= yield(:custom_js) %>

<%= render "shared/partials/custom_js" %>

<% p = controller.js_path %>
<% if (p.present?) %>
<script src="<%= p %>?<%= Constants::TS %>"></script>
<% end %>

</body>

</html>
