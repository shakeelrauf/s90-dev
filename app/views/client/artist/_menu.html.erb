<ul class="list-btns">
  <li>
    <a href="<%= artist_overview_artist_path(artist['id']) %>" class="btn btn--block <%=page =="overview" ? "current" : " "%> ajaxLink">Overview</a>
  </li>

  <li>
    <a href="<%= albums_artist_path(artist['id']) %>" class="btn btn--block <%=page =="albums" ? "current" : " "%> ajaxLink">Albums</a>
  </li>

  <li>
    <a href="<%= top_songs_artist_path(artist['id']) %>" class="btn btn--block <%=page =="top_songs" ? "current" : " "%> ajaxLink">Top Songs</a>
  </li>

  <li>
    <a href="<%= playlists_artist_path(artist['id']) %>" class="btn btn--block <%=page =="playlists" ? "current" : " "%> ajaxLink">Playlists</a>
  </li>
</ul><!-- /.list-btns -->